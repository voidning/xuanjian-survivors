const assert=require('assert/strict'),fs=require('fs'),vm=require('vm');const X=require('../scripts/field-event.js'),r=new X.Run(42);let strokes=0;const c=new Proxy({globalAlpha:1},{get:(o,k)=>k in o?o[k]:()=>{}}),w={};vm.runInNewContext(fs.readFileSync('scripts/clear-art.js','utf8'),{window:w,Math,Set});const snapshot=JSON.stringify(r);let outlines=0;const oval=(x,y,rx,ry,col,stroke)=>{if(stroke&&rx>100&&rx===ry)outlines++;};for(const reduced of [false,true])for(const kind of ['rain','edict','dusk','angler','peril','screen','fragrance']){const z={kind,x:r.p.x,y:r.p.y,r:180,age:1,life:5,fruitRain:true};const before=JSON.stringify(z);assert(w.XJClearArt.zone(c,r,z,()=>{},oval,()=>{},reduced));assert.equal(JSON.stringify(z),before);}assert.equal(outlines,2,'only one dusk outline per render');assert.equal(JSON.stringify(r),snapshot);assert(!w.XJClearArt.zone(c,r,{kind:'hazard'},()=>{},oval,()=>{},false));console.log('readability: deterministic geometry, one dusk edge, hazards delegated: passed');
