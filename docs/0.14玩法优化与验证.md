# 0.14 玩法优化与验证

日期：2026-09-08。范围：选卡适配、主修自动施放优先、单次限时山场试阵。

## 最终行为

持屏排除无效的镂金石选项，洞泉声仍可用于果位但停止无效耗蓝。适配卡片明确标出帝观元与谷风引火的使用前提。主修在目标和冷却允许时优先用法，保留紧急防御优先及 F 手动机制。90 秒出现试阵，140 秒结束；靠近才生成现有三类守卫，击败三人获得一次额外重抽。事件不修改原本固定参悟奖励、普通刷怪时间、首领时间或已有能力数值。试阵是游戏设计，不声称来自原著。

事件旗记已经移到地标旁空地，避免松亭树景遮挡；危险预警仍在友方效果上方。新标记无闪烁、旋转或新增粒子。修行录沿用原 schema 1 和存储键，只增加可选字段，旧档继续读取与合并。

## 基线与回归

开始和写入前核对 Git 工作区及旧文件哈希，保留无关内容。原有 `node tests/refinement.cjs` 基线 8/8 通过。

新增检查在实现前为 0/14：前两项确认持屏仍抽到镂金石、洞泉声会扣掉 22 法力；其余为尚未存在的新功能。原始结果保存为 `tests/playability-before.json`。补入持屏满级候选池回归后，最终 `node tests/playability.cjs` 为 **15/15**。覆盖适配、真实扣蓝、主修与手动互斥、紧急防御优先、蓄法和冷却让行、事件暂停、候选位置确定性、容量不足、击杀计数、超时、放弃、重复奖励、重开与继续无尽。

原有 `node tests/refinement.cjs` 最终 **8/8**；本轮改动涉及的 JS 语法和 HTML 本地资源引用检查通过。未将其他历史测试或旧报告中的数量、胜率算作本轮通过结果。

## 浏览器构造检查

在独立临时端口访问 `tests/playability-browser.html`，点击“运行界面与存档检查”，**13/13** 通过。覆盖提示实际显示、通过 DOM 选择主修和手动、HUD 状态、放弃事件、结算、修行录、序列化新字段和合并旧记录。已检查选卡、主修菜单和试阵标记的实际画面；首次标记有树景遮挡，移到空地后重新查看确认。

这是使用现有 opt-in test 接口的构造场景，不代表自然成型。测试页仅用于独立测试地址，运行会在该测试地址的浏览器存储中生成测试记录；不要在自己的正式存档地址运行它。

## 正常流程模拟与短局

`node tests/playability-smoke.cjs` 从实际起手、候选、经验和气血开始，不注入技能、经验、补血；只通过走位、选卡和设置主修推进。五件器物各两个种子，共 **10 局**，均运行至 150 秒并完成试阵。原始结果在 `tests/playability-smoke.json`。自动走位策略主动追求事件，因此不能用 10/10 推断真人完成率或事件难度。

另观察真实浏览器正常短局：青尺剑开局、自然击杀与受箓；该局于 01:49 死亡，结算显示试阵未接战。没有把此短局记为试阵完成或完整通关；此阶段没有完成十二分钟通关、长期平衡、跨设备帧率或听感验收。

## 使用

在原固定地址刷新页面并开新局，标题版本为 0.14.0。按 P 设置主修，01:30 后看右上角试阵提示。现有进行中的旧页面保留旧脚本，刷新会结束未结算战斗，请在本局结束后刷新。
